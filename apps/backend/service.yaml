apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: starter-kit

  labels:
    app.kubernetes.io/name: backend

spec:
  selector:
    app.kubernetes.io/name: backend

  ports:
    - name: http
      port: 8000
      targetPort: 8000

  type: ClusterIP